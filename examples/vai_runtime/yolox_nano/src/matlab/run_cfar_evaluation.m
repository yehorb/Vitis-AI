dsets = ["10to5" "5to0" "0to-5" "-5to-10" "-10to-15"];

for i = 1:length(dsets)
    dataset = "../../data/stft/" + dsets(i) + "dB";
    out_dir = "../../data/cfar_results/" + dsets(i);
    cfar_on_dataset(dataset, ...
        "OutDir", out_dir, ...
        "Split", "val", ...
        "NumSamples", 0, ...
        "PowerRecovery", 10, ...
        "RandomSamples", true, "SaveEveryN", 100, "LogEveryN", 100);
end
